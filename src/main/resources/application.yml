spring:
  application:
    name: system-pulse
  docker:
    compose:
      enabled: false
      skip:
        in-tests: false
      file: docker/docker-compose.db.yml
  datasource:
    url: jdbc:postgresql://localhost:5432/system-pulse-db
    username: user
    password: postgresdbpassword
  jpa:
    hibernate:
      ddl-auto: update
